# Core — pin exact versions to avoid resolver conflicts
apache-airflow==2.10.5
apache-airflow-providers-snowflake==5.8.0

# dbt — pin the full chain explicitly
dbt-snowflake==1.8.0
dbt-core==1.8.9
dbt-adapters==1.15.3
dbt-common==1.13.0

# Data ingestion
yfinance==0.2.51
finnhub-python==2.4.20
requests==2.32.3

# Cloud
boto3==1.35.0
snowflake-connector-python==3.12.3

# Utilities
python-dotenv==1.0.1
tenacity==9.0.0